dist: trusty
language: python
python:
  - "3.6"

# env:
#   global:
#     - DOCKER_COMPOSE_VERSION=1.19.0
install:
  - ls -l ../recipes/*json

script:
  - pytest
  - cd run
  - ls -l ../recipes/*json
  - python3 run.py
  - ls -l ../recipes/*json
